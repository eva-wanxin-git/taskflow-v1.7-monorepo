# 🎯 REQ-009最终方案 - 任务自动化流程

**确定时间**: 2025-11-19 01:30  
**架构师**: AI Architect (Expert Level)  
**基于**: 用户反馈 - 李明工作在新Cursor对话中  
**优先级**: 🔴 P0

---

## 📋 用户回答

### 关键信息
> 李明的工作环境是新的cursor，同一个任务如果发起来新的修复任务，我会给同一个cursor，否则每次都是不同的cursor

**理解**:
1. ✅ 李明 = AI（新Cursor对话）
2. ✅ 每个独立任务 = 新对话
3. ✅ 同任务修复 = 同对话（保持上下文）
4. ✅ 李明不能操作Dashboard UI

**推断**:
- ✅ 李明通过**文件交互**（读派发文档 → 生成完成报告）
- ✅ 用户复制粘贴在中间转接
- ✅ 完成报告命名约定（观察到：`✅REQ-XXX-完成报告.md`）

---

## 🎯 最终确定方案

### 方案：半自动化文件流程 ⭐ 最适配

**完整流程**:
```
步骤1：架构师派发任务
├─ 生成派发文档：📤派发给李明-{TASK系列}.md
├─ 录入数据库：任务状态 = pending
└─ 更新Dashboard事件流

步骤2：用户复制提示词
├─ 打开Dashboard → 待完成任务
├─ 点击"📋 一键复制提示词"按钮
├─ 系统读取派发文档内容
└─ 复制到剪贴板

步骤3：用户派发给李明
├─ 新建Cursor对话
├─ 粘贴提示词
└─ 李明AI开始工作

步骤4：李明更新状态（可选）
├─ 李明执行：python scripts/李明开始任务.py TASK-ID
├─ 调用API：PUT /api/tasks/{id}/start
└─ 数据库：pending → in_progress

步骤5：李明完成任务
├─ 李明生成：✅{TASK-ID}-完成报告.md
├─ 李明执行：python scripts/李明提交完成.py TASK-ID
├─ 调用API：POST /api/tasks/{id}/complete
└─ 数据库：in_progress → completed

步骤6：用户复制完成报告
├─ Dashboard显示"已完成"状态
├─ 点击"📄 一键复制完成报告"按钮
├─ 系统读取完成报告文件
└─ 复制到剪贴板

步骤7：用户粘贴给架构师
├─ 回到架构师对话
├─ 粘贴完成报告
└─ 架构师审查 → 通过/修改
```

**关键特点**:
- ✅ 李明通过文件和脚本操作（AI友好）
- ✅ 用户通过Dashboard一键复制（便捷）
- ✅ 状态更新自动化（API驱动）
- ✅ 保留审查环节（架构师把关）

---

## 🔧 需要实现的功能（4个）

### 功能1：Dashboard一键复制提示词 📋

**位置**: Dashboard待完成任务卡片

**前端按钮**:
```html
<!-- 待处理任务卡片 -->
<div class="task-card pending">
  <h3>[P0] TASK-C-1: 创建FastAPI主入口</h3>
  <p>描述: ...</p>
  <button onclick="copyTaskPrompt('TASK-C-1')" class="copy-btn">
    📋 一键复制提示词
  </button>
</div>
```

**JavaScript函数**:
```javascript
async function copyTaskPrompt(taskId) {
  try {
    // 调用后端API获取提示词
    const response = await fetch(`/api/tasks/${taskId}/prompt`);
    const data = await response.json();
    
    // 复制到剪贴板
    await navigator.clipboard.writeText(data.prompt);
    
    // 显示提示
    showToast('✅ 提示词已复制！可粘贴给李明AI');
  } catch (error) {
    showToast('❌ 复制失败: ' + error.message);
  }
}
```

**后端API**:
```python
@app.get("/api/tasks/{task_id}/prompt")
async def get_task_prompt(task_id: str):
    """获取任务派发提示词"""
    # 查找派发文档（支持多种命名）
    possible_files = [
        f"📤派发给李明-{task_id}.md",
        f"📤派发给李明-{task_id}系列.md",
        # 可能还有其他格式
    ]
    
    for filename in possible_files:
        filepath = Path(filename)
        if filepath.exists():
            with open(filepath, 'r', encoding='utf-8') as f:
                content = f.read()
            return {"task_id": task_id, "prompt": content}
    
    raise HTTPException(404, f"派发文档未找到: {task_id}")
```

---

### 功能2：李明更新任务状态脚本 🔄

**脚本**: `scripts/李明开始任务.py`

```python
#!/usr/bin/env python3
"""
李明开始任务 - 更新状态为进行中
用法：python scripts/李明开始任务.py TASK-C-1
"""

import sys
import requests
from datetime import datetime

if len(sys.argv) < 2:
    print("用法: python 李明开始任务.py <TASK-ID>")
    sys.exit(1)

task_id = sys.argv[1]

# 调用API更新状态
response = requests.put(
    f"http://localhost:8877/api/tasks/{task_id}/start",
    json={"started_by": "fullstack-engineer"}
)

if response.status_code == 200:
    print(f"✅ 任务 {task_id} 已开始！")
    print(f"   状态: 待处理 → 进行中")
    print(f"   时间: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")
else:
    print(f"❌ 更新失败: {response.text}")
```

**后端API**:
```python
@app.put("/api/tasks/{task_id}/start")
async def start_task(task_id: str):
    """开始任务（待处理→进行中）"""
    conn = sqlite3.connect("database/data/tasks.db")
    cursor = conn.cursor()
    
    cursor.execute("""
        UPDATE tasks 
        SET status = 'in_progress', 
            updated_at = ? 
        WHERE id = ?
    """, (datetime.now().isoformat(), task_id))
    
    conn.commit()
    conn.close()
    
    return {
        "success": True,
        "task_id": task_id,
        "status": "in_progress"
    }
```

---

### 功能3：李明提交完成报告脚本 ✅

**脚本**: `scripts/李明提交完成.py`

```python
#!/usr/bin/env python3
"""
李明提交任务完成 - 更新状态为已完成
用法：python scripts/李明提交完成.py TASK-C-1
"""

import sys
import requests
from pathlib import Path
from datetime import datetime

if len(sys.argv) < 2:
    print("用法: python 李明提交完成.py <TASK-ID>")
    sys.exit(1)

task_id = sys.argv[1]

# 查找完成报告（支持多种命名）
report_patterns = [
    f"✅{task_id}-完成报告.md",
    f"{task_id}-完成报告.md",
    f"完成报告-{task_id}.md"
]

report_path = None
for pattern in report_patterns:
    if Path(pattern).exists():
        report_path = pattern
        break

if not report_path:
    print(f"❌ 错误：找不到完成报告文件")
    print(f"   期望文件名：✅{task_id}-完成报告.md")
    sys.exit(1)

# 调用API提交完成
response = requests.post(
    f"http://localhost:8877/api/tasks/{task_id}/complete",
    json={
        "report_path": report_path,
        "completed_by": "fullstack-engineer"
    }
)

if response.status_code == 200:
    print(f"✅ 任务 {task_id} 已提交完成！")
    print(f"   状态: 进行中 → 已完成")
    print(f"   报告: {report_path}")
    print(f"   时间: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")
    print()
    print("📊 请在Dashboard查看已完成任务")
    print("📄 点击'一键复制完成报告'按钮，粘贴给架构师审查")
else:
    print(f"❌ 提交失败: {response.text}")
```

**后端API**:
```python
@app.post("/api/tasks/{task_id}/complete")
async def complete_task(task_id: str, request: Request):
    """提交任务完成（进行中→已完成）"""
    body = await request.json()
    report_path = body.get("report_path")
    
    # 更新数据库
    conn = sqlite3.connect("database/data/tasks.db")
    cursor = conn.cursor()
    
    # 构造metadata
    metadata = json.dumps({
        "completion_report": report_path,
        "completed_by": body.get("completed_by"),
        "completed_at": datetime.now().isoformat()
    }, ensure_ascii=False)
    
    cursor.execute("""
        UPDATE tasks 
        SET status = 'completed', 
            updated_at = ?,
            metadata = json_patch(metadata, ?)
        WHERE id = ?
    """, (datetime.now().isoformat(), metadata, task_id))
    
    conn.commit()
    conn.close()
    
    return {
        "success": True,
        "task_id": task_id,
        "status": "completed",
        "report_path": report_path
    }
```

---

### 功能4：Dashboard一键复制完成报告 📄

**前端按钮**:
```html
<!-- 已完成任务卡片 -->
<div class="task-card completed">
  <h3>[P0] TASK-C-1: 创建FastAPI主入口</h3>
  <p>✅ 完成时间: 2025-11-19 12:00</p>
  <button onclick="copyCompletionReport('TASK-C-1')" class="copy-btn">
    📄 一键复制完成报告
  </button>
</div>
```

**JavaScript函数**:
```javascript
async function copyCompletionReport(taskId) {
  try {
    // 调用后端API获取完成报告
    const response = await fetch(`/api/tasks/${taskId}/report`);
    const data = await response.json();
    
    // 复制到剪贴板
    await navigator.clipboard.writeText(data.report);
    
    // 显示提示
    showToast('✅ 完成报告已复制！可粘贴给架构师审查');
  } catch (error) {
    showToast('❌ 复制失败: ' + error.message);
  }
}
```

**后端API**:
```python
@app.get("/api/tasks/{task_id}/report")
async def get_completion_report(task_id: str):
    """获取任务完成报告"""
    # 支持多种命名格式
    possible_files = [
        f"✅{task_id}-完成报告.md",
        f"{task_id}-完成报告.md",
        f"完成报告-{task_id}.md"
    ]
    
    for filename in possible_files:
        filepath = Path(filename)
        if filepath.exists():
            with open(filepath, 'r', encoding='utf-8') as f:
                content = f.read()
            return {"task_id": task_id, "report": content}
    
    raise HTTPException(404, f"完成报告未找到: {task_id}")
```

---

## 📋 文件命名约定（基于观察）

### 派发文档
**格式**: `📤派发给李明-{任务系列}.md`

**示例**:
- `📤派发给李明-TASK-C系列.md`（3个任务：C-1, C-2, C-3）
- `📤派发给李明-REQ-002-B修复任务.md`（单个任务）

### 完成报告
**格式**: `✅{TASK-ID}-完成报告.md`

**示例**:
- `✅REQ-001-完成报告.md`（已观察到）
- `✅REQ-002-项目记忆空间-完成报告.md`（已观察到）
- `✅REQ-003-对话历史库功能-完成报告.md`（已观察到）

**位置**: 项目根目录

---

## 🎯 任务拆解

### REQ-009-A: Dashboard一键复制功能 🔴 P0

**工时**: 2小时  
**派发给**: 全栈工程师·李明

**任务内容**:
1. 添加"一键复制提示词"按钮（待处理任务）
2. 添加"一键复制完成报告"按钮（已完成任务）
3. 实现JavaScript复制函数（2个）
4. 实现后端API（2个）

**验收标准**:
- [ ] 待处理任务卡片有"📋 一键复制提示词"按钮
- [ ] 已完成任务卡片有"📄 一键复制完成报告"按钮
- [ ] 点击按钮可复制到剪贴板
- [ ] 显示成功提示
- [ ] API返回正确内容

---

### REQ-009-B: 李明状态管理脚本 🔴 P0

**工时**: 1小时  
**派发给**: 全栈工程师·李明

**任务内容**:
1. 创建`scripts/李明开始任务.py`
2. 创建`scripts/李明提交完成.py`
3. 实现后端API（2个端点）
4. 编写使用文档

**验收标准**:
- [ ] 2个脚本可运行
- [ ] API端点正常工作
- [ ] 数据库状态正确更新
- [ ] 有使用说明

---

### REQ-009-C: Dashboard状态自动刷新 🟡 P1

**工时**: 1小时  
**派发给**: 全栈工程师·李明

**任务内容**:
1. 添加轮询机制（每5秒）
2. 检测任务状态变化
3. 自动刷新任务列表
4. 无需手动刷新页面

**验收标准**:
- [ ] Dashboard每5秒自动刷新任务
- [ ] 状态变化立即可见
- [ ] 不影响用户操作

---

## 📊 实施优先级

**立即实施**（P0，3小时）:
- REQ-009-A: Dashboard一键复制（2h）
- REQ-009-B: 李明脚本（1h）

**后续实施**（P1，1小时）:
- REQ-009-C: 自动刷新（1h）

**总工时**: 4小时

---

## 💡 架构师洞察

### 为什么选择这个方案？

1. **适配AI工作方式** ✅
   - 李明是AI，通过文件和脚本操作最自然
   - 不需要UI交互

2. **保留人工审查** ✅
   - 用户中转（复制粘贴）
   - 架构师审查（质量把关）
   - 不是完全自动化（有监督）

3. **开发成本合理** ✅
   - 4小时开发
   - 技术简单（API + 文件读取 + 剪贴板）

4. **可扩展** ✅
   - 未来可升级到完全自动化
   - 未来可添加UI操作支持

---

## 📋 下一步行动

### 1. 派发REQ-009任务给李明
- 创建派发文档
- 录入数据库
- 更新Dashboard

### 2. 继续处理其他需求
- REQ-004：即插即用流程（需要深度分析）
- REQ-005：Dashboard重构（需要拆解）

---

**方案确定完成！** ✅

**基于用户反馈**: 李明工作在新Cursor对话中  
**最终方案**: 半自动化文件流程（4小时开发）  
**下一步**: 立即拆解并派发REQ-009任务

---

**架构师**: AI Architect (Expert Level)  
**时间**: 2025-11-19 01:30  
**Token**: 147K/1M (14.7%)  
**Dashboard**: http://localhost:8877

