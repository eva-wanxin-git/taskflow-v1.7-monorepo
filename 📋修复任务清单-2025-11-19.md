# 📋 修复任务清单 - 2025-11-19

**生成时间**: 2025-11-19 00:15  
**架构师**: AI Architect (Expert Level)  
**背景**: 今晚实现了大量功能但Dashboard未正确展示  
**目标**: 修复展示问题，确保数据正确接入

---

## 🎯 问题诊断

### 核心问题
```
数据库状态: 54任务, 25已完成 (46.3%)
task-board.md: 只显示11任务 (60%)
Dashboard展示: 可能未同步最新数据

差异原因:
1. task-board.md未更新同步所有数据库任务
2. Dashboard读取的可能是过期task-board.md
3. 今晚新增的14个INTEGRATE任务未在task-board显示
```

### 数据库实际状态(已验证)
```
总任务: 54个
✅ 已完成: 25个 (46.3%)
🟡 进行中: 1个  (INTEGRATE-003)
⏳ 待处理: 24个
❌ 已取消: 4个
```

---

## 🔧 修复任务列表

### FIX-001: 同步task-board.md与数据库 🔴 P0

**工时**: 2h  
**复杂度**: MEDIUM  
**依赖**: 无  
**状态**: 🟡 待处理

#### 任务目标
将数据库中的54个任务完整同步到task-board.md，确保文档与数据一致。

#### 技术要点
```python
# 1. 读取数据库所有任务
SELECT id, title, description, status, priority, estimated_hours, 
       assigned_to, dependencies, created_at, completed_at
FROM tasks
ORDER BY 
  CASE priority
    WHEN 'P0' THEN 1
    WHEN 'P1' THEN 2
    WHEN 'P2' THEN 3
  END,
  created_at;

# 2. 按状态分组
tasks_by_status = {
    'completed': [...],  # 25个
    'in_progress': [...],  # 1个
    'pending': [...],  # 24个
    'cancelled': [...]  # 4个
}

# 3. 生成Markdown格式
# 4. 更新task-board.md
```

#### 验收标准
- [ ] task-board.md显示54个任务（不是11个）
- [ ] 按优先级和状态正确分组
- [ ] 已完成任务列表准确（25个）
- [ ] 包含所有INTEGRATE-001到INTEGRATE-014
- [ ] 包含所有REQ系列和TASK系列
- [ ] Markdown格式规范

#### 参考资料
- 数据库: `database/data/tasks.db`
- 当前文档: `docs/tasks/task-board.md`
- 脚本: `scripts/修正所有任务状态.py` (可参考)

---

### FIX-002: 修复Dashboard数据读取 🔴 P0

**工时**: 1.5h  
**复杂度**: LOW  
**依赖**: FIX-001  
**状态**: 🟡 待处理

#### 任务目标
确保Dashboard直接从数据库读取任务，不依赖过期的文件缓存。

#### 技术要点
```python
# apps/dashboard/src/industrial_dashboard/dashboard.py

@app.get("/api/tasks")
async def get_tasks():
    # 当前可能的问题点
    state_manager = StateManager()
    tasks = state_manager.list_all_tasks()  # 确认这里真的查数据库
    
    # 验证
    print(f"DEBUG: 返回{len(tasks)}个任务")
    
    return JSONResponse(content=tasks)
```

#### 验收标准
- [ ] `/api/tasks`返回54个任务
- [ ] Dashboard显示任务数量正确: 54
- [ ] 进度计算正确: 25/54 = 46.3%
- [ ] 所有INTEGRATE任务可见
- [ ] 所有REQ/TASK任务可见

#### 调试步骤
```bash
# 1. 启动Dashboard
cd taskflow-v1.7-monorepo/apps/dashboard
python start_dashboard.py

# 2. 测试API
curl http://localhost:8877/api/tasks | jq 'length'
# 预期: 54

# 3. 检查浏览器
# 打开 http://localhost:8877
# F12 Console看请求
# Network Tab看/api/tasks返回的数量
```

---

### FIX-003: 创建完整任务清单展示页 🟡 P1

**工时**: 2h  
**复杂度**: MEDIUM  
**依赖**: FIX-001, FIX-002  
**状态**: 🟡 待处理

#### 任务目标
在Dashboard添加"完整任务清单"Tab，展示所有54个任务的详细信息。

#### 技术要点
```javascript
// 新增Tab: 完整任务清单
function renderFullTaskList(tasks) {
    const grouped = {
        completed: tasks.filter(t => t.status === 'completed'),
        in_progress: tasks.filter(t => t.status === 'in_progress'),
        pending: tasks.filter(t => t.status === 'pending'),
        cancelled: tasks.filter(t => t.status === 'cancelled')
    };
    
    // 分4个section展示
    return `
        <h3>✅ 已完成 (${grouped.completed.length})</h3>
        ${renderTaskCards(grouped.completed)}
        
        <h3>🟡 进行中 (${grouped.in_progress.length})</h3>
        ${renderTaskCards(grouped.in_progress)}
        
        <h3>⏳ 待处理 (${grouped.pending.length})</h3>
        ${renderTaskCards(grouped.pending)}
        
        <h3>❌ 已取消 (${grouped.cancelled.length})</h3>
        ${renderTaskCards(grouped.cancelled)}
    `;
}
```

#### 验收标准
- [ ] Dashboard新增"任务清单"Tab
- [ ] 显示4个section(已完成/进行中/待处理/已取消)
- [ ] 每个任务卡片显示: ID/标题/优先级/工时/负责人
- [ ] 点击任务ID可复制
- [ ] 支持按状态筛选
- [ ] 支持按优先级排序

---

### FIX-004: 验证已完成任务的文件存在性 🟡 P1

**工时**: 1h  
**复杂度**: LOW  
**依赖**: 无  
**状态**: 🟡 待处理

#### 任务目标
检查数据库标记为completed的25个任务，是否都有对应的完成报告文件。

#### 技术要点
```python
import sqlite3
from pathlib import Path

db = sqlite3.connect('database/data/tasks.db')
cursor = db.cursor()

# 获取已完成任务
cursor.execute("SELECT id, title FROM tasks WHERE status='completed'")
completed_tasks = cursor.fetchall()

# 检查文件
missing = []
for task_id, title in completed_tasks:
    # 查找可能的完成报告文件名
    patterns = [
        f"*{task_id}*完成报告*.md",
        f"✅{task_id}*.md",
        f"*{task_id}-完成*.md"
    ]
    
    found = False
    for pattern in patterns:
        if list(Path(".").glob(pattern)):
            found = True
            break
    
    if not found:
        missing.append((task_id, title))

# 生成报告
print(f"已完成任务: {len(completed_tasks)}")
print(f"缺少完成报告: {len(missing)}")
for task_id, title in missing:
    print(f"  - {task_id}: {title}")
```

#### 验收标准
- [ ] 扫描所有已完成任务(25个)
- [ ] 列出缺少完成报告的任务
- [ ] 生成Markdown报告
- [ ] 如果有缺失，标记为待补充

---

### FIX-005: 修复事件流完整性 🟡 P1

**工时**: 1h  
**复杂度**: LOW  
**依赖**: 无  
**状态**: 🟡 待处理

#### 任务目标
验证154个事件记录是否完整，是否所有任务状态变更都有对应事件。

#### 技术要点
```python
import json
from datetime import datetime

# 1. 读取事件流
with open('apps/dashboard/automation-data/architect_events.json', 'r') as f:
    events_data = json.load(f)
    events = events_data.get('events', [])

# 2. 统计事件类型
event_types = {}
for event in events:
    event_type = event.get('event_type', 'unknown')
    event_types[event_type] = event_types.get(event_type, 0) + 1

# 3. 检查关键事件
task_created_events = [e for e in events if e['event_type'] == 'task_created']
task_completed_events = [e for e in events if e['event_type'] == 'task_completed']

# 4. 对比数据库
# 数据库有25个completed，事件流应该有25个task_completed

print(f"总事件数: {len(events)}")
print(f"事件类型分布: {event_types}")
print(f"任务创建事件: {len(task_created_events)}")
print(f"任务完成事件: {len(task_completed_events)}")
```

#### 验收标准
- [ ] 事件流JSON文件可正常读取
- [ ] 154个事件数量正确
- [ ] 25个任务完成事件对应25个completed任务
- [ ] 事件时间戳连续无断层
- [ ] 生成事件流健康报告

---

### FIX-006: 生成功能实现总览文档 🟢 P2

**工时**: 2h  
**复杂度**: MEDIUM  
**依赖**: FIX-001, FIX-004  
**状态**: 🟡 待处理

#### 任务目标
基于已完成的25个任务和完成报告，生成一份功能实现总览文档。

#### 技术要点
```markdown
# 任务所·Flow v1.7 - 功能实现总览

## 📊 总体进度
- 总任务: 54个
- 已完成: 25个 (46.3%)
- 进行中: 1个
- 待处理: 24个

## ✅ 已实现功能清单 (25个)

### 基础设施 (7个)
1. REQ-001: 端口冲突彻底解决方案
   - 功能: 版本号URL参数、no-cache HTTP头、Service Worker
   - 位置: packages/shared-utils/version_cache_manager.py
   - 完成报告: REQ-001-完成报告.md

2. REQ-002: 项目记忆空间创立
   - 功能: 4表知识图谱、项目记忆API
   - 位置: packages/core-domain/src/services/project_memory_service.py
   - 完成报告: ✅REQ-002-项目记忆空间-完成报告.md

...（依次列出25个）

### 集成任务 (5个)
...

### 子任务 (11个)
...

### 问题修复 (2个)
...
```

#### 验收标准
- [ ] 文档包含所有25个已完成任务
- [ ] 每个任务有: 功能描述/代码位置/完成报告链接
- [ ] 按类别分组: 基础/集成/子任务/修复
- [ ] 生成统计图表(可选)
- [ ] 保存为Markdown文档

---

##测FIX-007: Dashboard添加任务筛选功能 🟢 P2

**工时**: 1.5h  
**复杂度**: MEDIUM  
**依赖**: FIX-002, FIX-003  
**状态**: 🟡 待处理

#### 任务目标
在Dashboard顶部添加筛选器,支持按状态/优先级/类型筛选任务。

#### 技术要点
```javascript
// 筛选器UI
<div class="task-filters">
  <select id="statusFilter">
    <option value="all">全部状态</option>
    <option value="completed">已完成</option>
    <option value="in_progress">进行中</option>
    <option value="pending">待处理</option>
  </select>
  
  <select id="priorityFilter">
    <option value="all">全部优先级</option>
    <option value="P0">P0</option>
    <option value="P1">P1</option>
    <option value="P2">P2</option>
  </select>
  
  <select id="typeFilter">
    <option value="all">全部类型</option>
    <option value="REQ">REQ</option>
    <option value="INTEGRATE">INTEGRATE</option>
    <option value="TASK">TASK</option>
  </select>
</div>

// 筛选逻辑
function filterTasks() {
    const status = document.getElementById('statusFilter').value;
    const priority = document.getElementById('priorityFilter').value;
    const type = document.getElementById('typeFilter').value;
    
    let filtered = allTasks;
    
    if (status !== 'all') {
        filtered = filtered.filter(t => t.status === status);
    }
    if (priority !== 'all') {
        filtered = filtered.filter(t => t.priority === priority);
    }
    if (type !== 'all') {
        filtered = filtered.filter(t => t.id.startsWith(type));
    }
    
    renderTaskList(filtered);
    updateFilterCounts(filtered);
}
```

#### 验收标准
- [ ] Dashboard顶部显示3个筛选下拉框
- [ ] 按状态筛选正常(all/completed/in_progress/pending)
- [ ] 按优先级筛选正常(all/P0/P1/P2)
- [ ] 按类型筛选正常(all/REQ/INTEGRATE/TASK)
- [ ] 显示筛选结果数量: "显示X/54个任务"
- [ ] 筛选器样式符合工业美学

---

## 📋 执行顺序建议

### 第一批 (立即): 数据修复
```
优先级P0，确保数据正确
├── FIX-001: 同步task-board.md (2h)
└── FIX-002: 修复Dashboard读取 (1.5h)
```

### 第二批 (今天): 展示优化
```
优先级P1，改善展示
├── FIX-003: 完整任务清单Tab (2h)
├── FIX-004: 验证文件存在性 (1h)
└── FIX-005: 检查事件流 (1h)
```

### 第三批 (可选): 功能增强
```
优先级P2，锦上添花
├── FIX-006: 生成总览文档 (2h)
└── FIX-007: 添加筛选功能 (1.5h)
```

**总工时**: 11.5h  
**建议分配**: 2天完成前两批，第三批可延后

---

## 🎯 预期成果

完成所有修复后:
- ✅ task-board.md显示完整54个任务
- ✅ Dashboard显示46.3%真实进度
- ✅ 所有INTEGRATE/REQ/TASK任务可见
- ✅ 任务清单Tab显示详细信息
- ✅ 筛选功能方便查看不同类别
- ✅ 事件流和数据库一致

---

## 📤 如何派发这些任务

### 方式1: 批量派发给李明
```markdown
创建文件: 📤派发给李明-修复任务FIX-001到007.md

内容包含:
1. 7个任务的完整描述
2. 验收标准清单
3. 执行顺序建议
4. 完成报告要求

李明执行:
1. 读取派发文档
2. 按顺序执行FIX-001到FIX-007
3. 每完成一个提交完成报告
```

### 方式2: 逐个派发
```markdown
先派发FIX-001+FIX-002 (P0紧急)
等完成后再派发FIX-003到FIX-005 (P1重要)
最后派发FIX-006+FIX-007 (P2可选)
```

### 方式3: 架构师自己修
```markdown
如果修复简单,架构师可以直接修复FIX-001和FIX-002
然后把剩余任务派发给李明
```

---

## ⚠️ 关键提示

1. **FIX-001和FIX-002必须先完成**
   - 这两个是数据正确性的基础
   - 其他任务都依赖这两个

2. **修复过程中保持备份**
   - 修改task-board.md前先备份
   - 修改Dashboard前先测试

3. **逐步验证**
   - 每完成一个FIX，立即验证
   - 不要等所有完成再测试

4. **记录到数据库**
   - 所有FIX任务都要记录到tasks表
   - 状态流转要完整

---

**生成时间**: 2025-11-19 00:15  
**任务数量**: 7个修复任务  
**总工时**: 11.5h  
**架构师**: AI Architect (Expert Level)  
**状态**: ✅ 清单已生成，待派发

